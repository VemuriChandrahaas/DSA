Aim:To write a program for an element in a sorted array using linear search technique


#include <stdio.h>

int main()
{
    int n, key, i;

    // Ask the user how many elements they want in the array
    printf("Enter the number of elements required: ");
    scanf("%d", &n);

    // Create an array of size n
    int array[n];

    // Take input for each element of the array
    printf("Enter the elements:\n");
    for(i = 0; i < n; i++)
    {
        scanf("%d", &array[i]);
    }

    // Ask the user for the key element to search
    printf("Enter the key to search for: ");
    scanf("%d", &key);

    // Linear search: go through each element
    for(i = 0; i < n; i++)
    {
        // NOTE: The program checks (key + 1), not key directly
        // This means if key = 5, it searches for 6 in the array
        if(array[i] == key + 1)
        {
            // Print the position where the element is found
            printf("Element found at position %d\n", i);
            break;
        }
    }

    // If loop completed without break → element not found
    if(i == n)
    {
        printf("Element not found\n");
    }

    return 0;
}


output:
Enter the number of elements required: 5
Enter the elements:
2
4
6
8
10
Enter the key to search for: 6
Element found at position 2

Result: 
        The program successfully searches for a given key element in a sorted array 
using the linear search technique and displays whether the element is found or not.
