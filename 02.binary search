Aim:To write a program for an element in a sorted array using binary search technique


#include<stdio.h>

int main()
{
    int i, n, key;

    // Ask user for number of elements
    printf("enter the number of elements n: \n");
    scanf("%d", &n);
    
    // Create an array of size n
    int array[n];

    // Take input for array elements
    // NOTE: For binary search, the array must be sorted
    printf("enter the elements: \n");
    for(i = 0; i < n; i++)
    {
        scanf("%d", &array[i]);
    }

    // Ask for the key element to search
    printf("enter the key \n");
    scanf("%d",&key);

    // Declare variables for binary search
    int mid, start = 0, end = n - 1;
    
    // Binary search loop
    while(start <= end)
    {
        // Find the middle index
        mid = (start + end) / 2;

        // Print current mid and key value for understanding
        printf("\nmid = %d, key = %d", mid, key);

        // If middle element matches the key → element found
        if(array[mid] == key)
        {
            printf("\nfound at position %d", mid + 1);   // Position = index + 1
            break;
        }

        // If key is greater → search right half
        if(array[mid] < key)
        {
            start = mid + 1;
        }
        // If key is smaller → search left half
        else
        {
            end = mid - 1;
        }
    }

    // If start goes beyond end → key not present
    if(start > end){
        printf("\nnot found");
    }

    return 0;
}


output:
enter the number of elements n:
5
enter the elements:
2
4
6
8
10
enter the key
8

mid-2, Key-8
mid-3, Key-8
found at position 4

result:
The program successfully implements the Binary Search technique
to locate a key element in a sorted array and displays whether the element is present or not.


